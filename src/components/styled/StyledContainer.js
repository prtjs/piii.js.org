import styled from 'styled-components'

// Design
import { xlWidth } from '../design/devices'

const StyledContainer = styled.div`
  margin: 0 auto;
  padding: 0 20px;
  max-width: ${xlWidth + 40}px;
`

export default StyledContainer
